# Chỉ cần file .env khi frontend và backend KHÁC domain.
# Cùng domain + proxy: KHÔNG cần .env (build mặc định gọi relative /api/...).

# Ví dụ backend riêng domain:
# UMI_APP_API_URL=https://api.tendoanhnghiep.com
