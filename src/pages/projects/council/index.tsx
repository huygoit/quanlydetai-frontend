/**
 * Hội đồng xét duyệt
 */
import { PageContainer } from '@ant-design/pro-components';
import { Card } from 'antd';

const ProjectCouncilPage: React.FC = () => {
  return (
    <PageContainer>
      <Card>
        <div style={{ textAlign: 'center', padding: '40px 0', color: '#999' }}>
          👥 Hội đồng xét duyệt - Đang phát triển
        </div>
      </Card>
    </PageContainer>
  );
};

export default ProjectCouncilPage;
